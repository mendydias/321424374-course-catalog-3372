[project]
name = "workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["textual>=8.2.8"]

[dependency-groups]
dev = [
    "hypothesis>=6.161.5",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]
